
@{
    ViewBag.Title = "check";
}

<h2>check</h2>
@{
    if (ViewData["tested"].Equals("true"))
    {
        using (Html.BeginForm("addplayer", "admin", new { area = "Admin" }, FormMethod.Get))
        {
            <input type="submit" class="btn btn-info" id="addplayer" name="addplayer" value="ADD PLAYER">
        }
        using (Html.BeginForm("editplayer", "admin", new { area = "Admin" }, FormMethod.Get))
        {
            <input type="submit" class="btn btn-info" id="edit" name="edit" value="EDIT">
        }
        using (Html.BeginForm("deleteplayer", "admin", new { area = "Admin" }, FormMethod.Get))
        {
            <input type="submit" class="btn btn-info" id="deleteplayer" name="deleteplayer" value="DELETE">
        }
        using (Html.BeginForm("startauction", "admin", new { area = "Admin" }, FormMethod.Get))
        {
            <input type="submit" class="btn btn-info" id="start" name="start" value="START">
        }
    }
    else
    {
        using (Html.BeginForm("Index", "admin", new { area = "Admin" }, FormMethod.Get))
        {
            <h1>Please enter correct details</h1>
            <input type="submit" id="back" name="back" value="BACK">

        }
    }
}